const { check, body, query, param, custom, validationResult } = require('express-validator')

export let user = {
    userGetOne: [

    ],
    userPostOne: [

    ],
    userPutOne: [

    ],
    userDeleteOne: [

    ],
}
